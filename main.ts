// 后端
import * as http from "http"
import * as fs from "fs";
import * as vm from "vm";

/**
 * 主函数
 * 这个函数提供了TSNB的后端服务器支持,
 * 包括HTTP/HTTPS静态访问和数据上传
 */
(()=>{
    var Config:LaunchParameter = {
        ConfigFile:"tsnb.config.js"
    }
    process.argv.forEach(()=>{
    })
})();